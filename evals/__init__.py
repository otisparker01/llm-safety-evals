"""Safety evals. Each subpackage is a self-contained eval family.

Existing: `oversight` (Anthropic oversight-failure suite: sandbagging, CoT
faithfulness, CoT perturbation). Add new eval families as sibling subpackages,
e.g. `evals/<name>/`.
"""
